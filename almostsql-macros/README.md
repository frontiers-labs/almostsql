# almostsql-macros

Procedural macros used by [`almostsql`](https://crates.io/crates/almostsql).

This crate is an implementation detail. Applications should depend on
`almostsql`, which re-exports the `migrations!` macro.
